for_window [class="^.*"] border pixel 1

set $mod Mod4
set $i3lockwall i3lock --show-failed-attempts --tiling -i ~/backgrounds/trees.png

exec_always --no-startup-id xrandr --output eDP-1 --primary --mode 1920x1080 \
       --output HDMI-1 --mode 1366x768 --above eDP-1

exec --no-startup-id xinput set-prop "GXT7863:00 27C6:01E0 Touchpad" "libinput Tapping Enabled" 1
exec --no-startup-id xinput set-prop "GXT7863:00 27C6:01E0 Touchpad" "libinput Natural Scrolling Enabled" 1
exec --no-startup-id nitrogen --restore
exec --no-startup-id xrdb -merge ~/.Xresources
exec --no-startup-id xss-lock --transfer-sleep-lock -- $i3lockwall
exec --no-startup-id nm-applet
exec --no-startup-id dex --autostart --environment i3

# Volume (PipeWire, fixed)
bindsym XF86AudioRaiseVolume exec --no-startup-id wpctl set-volume 54 5%+
bindsym XF86AudioLowerVolume exec --no-startup-id wpctl set-volume 54 5%-
bindsym XF86AudioMute exec --no-startup-id wpctl set-mute 54 toggle

# Mic
bindsym XF86AudioMicMute exec --no-startup-id wpctl set-mute 56 toggle

# Brightness
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +10%
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 10%-

# Lock
bindsym $mod+x exec i3lock --show-failed-attempts -i ~/backgrounds/demon.jpg


# Font
font pango:JetBrainsMono Nerd Font 14

# Launchers
bindsym $mod+Return exec alacritty
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+Tab exec --no-startup-id rofi -show window
bindsym $mod+v exec pavucontrol

# kill focused window 
bindsym $mod+q kill

# Screenshots
bindsym Print exec maim ~/Pictures/$(date +%Y-%m-%d_%H-%M-%S).png
bindsym Shift+Print exec maim --select ~/Pictures/$(date +%Y-%m-%d_%H-%M-%S).png
bindsym Ctrl+Print exec maim | xclip -selection clipboard -t image/png
bindsym Ctrl+Shift+Print exec maim --select | xclip -selection clipboard -t image/png

# Focus / Move
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# Layouts
bindsym $mod+f fullscreen toggle
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+t layout toggle split
bindsym $mod+space floating toggle

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Reload, restart, exit
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Exit i3?' -B 'Yes' 'i3-msg exit'"

# Resize mode
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# set primary gruvbox colorscheme colors
set $bg #282828
set $red #cc241d
set $green #98971a
set $yellow #d79921
set $blue #458588
set $purple #b16286
set $aqua #689d68
set $gray #a89984
set $darkgray #1d2021

# Green gruvbox
# class                 border|backgr|text|indicator|child_border
client.focused          $green $green $darkgray $green $green
client.focused_inactive $darkgray $darkgray $yellow $darkgray $darkgray
client.unfocused        $darkgray $darkgray $yellow $darkgray $darkgray
client.urgent           $red $red $white $red $red


bar {
    status_command i3status --config ~/.config/i3/i3status.conf
    tray_output none
    font pango:JetBrainsMono Nerd Font 13

    position bottom

 i3bar_command i3bar --transparency

 colors {
        # bar background color
        background $bg
        # text color used for blocks that do not have a color specified.
        statusline $yellow
        # workspaces section            
        #                    border  backgr. text
        focused_workspace    $aqua $aqua $darkgray
        inactive_workspace   $bg $bg $yellow
        active_workspace     $bg $bg $yellow
        urgent_workspace     $red $red $bg
    }
}

# picom
exec_always --no-startup-id picom --config ~/.config/picom/picom.conf --xrender-sync-fence

# disable screen turning off
exec_always --no-startup-id xset s 0 0
